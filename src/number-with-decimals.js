/**
 * Floating Precision
 * Floating point arithmetic is not always 100% accurate
 */

const precision = 100;
const num1 = 0.1 * precision;
const num2 = 0.2 * precision;
console.log((num1 + num2) / 100);
